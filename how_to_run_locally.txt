⚡ How to Run Locally

Follow these steps to get the service running on your computer.
1. Install Dependencies
1. Install Dependencies

Open your terminal in this folder and run:
Bash

pip install -r requirements.txt

2. Start the Server

Run the application using Uvicorn:
Bash

uvicorn main:app --reload

You should see a message saying: Uvicorn running on http://127.0.0.1:8000
3. Test the API

Once the server is running, open your browser and go to: 👉 http://127.0.0.1:8000/docs

This is the interactive dashboard where you can test the 3 endpoints:

    POST /log_event - Record a new event.

    GET /events/{user_id} - Fetch events for a user.

    GET /events/{user_id}/{concept_id} - Fetch concept-specific events.

🗄️ Database

This project uses SQLite. You do not need to install anything extra. The file learning_events_v2.db will be created automatically when you run the app for the first time.